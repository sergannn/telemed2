name: doctorq
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: ^3.4.3

executables: 
  code_uml:
dependencies:
  flutter:
    sdk: flutter
  file_picker: ^9.2.0
  http_parser: 

  cupertino_icons: ^1.0.8
  collection:
  # daily_flutter: ^0.28.1  # УДАЛЕНО для веб-версии
     
  freezed_annotation: ^2.2.0
  json_annotation: ^4.7.0
  logging:
  shared_preferences: ^2.3.2
  accordion: ^2.5.1
  animate_do: ^3.3.4
  cached_network_image: ^3.2.1
  # camera: ^0.10.5+9  # УДАЛЕНО для веб-версии
  carousel_slider: ^4.0.0
  date_picker_timeline: ^1.2.6
  easy_localization: ^3.0.8
    
  flutter_launcher_icons: ^0.13.1
  flutter_rating_bar: ^4.0.1
  flutter_spinkit: ^5.1.0
  flutter_svg: ^2.0.10+1
  flutter_switch: ^0.3.2
  http: ^1.2.1
  outline_gradient_button: ^2.0.0+1
  # permission_handler: ^11.3.1  # УДАЛЕНО для веб-версии
  persistent_bottom_nav_bar_v2: ^5.2.3
  pin_code_fields: ^8.0.1
  pubnub: ^4.3.4
  smooth_page_indicator: ^1.0.0+2
  url_launcher: ^6.3.0
  get: ^4.6.6
  get_it:
  graphql: ^5.2.0-beta.8
  mobx: ^2.3.3+2
  html_unescape: ^2.0.0
  date_count_down: ^3.0.0
  story_view: ^0.16.5
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  # image_picker: ^1.1.2  # Может не работать на веб
  flutter_cache_manager: ^3.4.1
  # vibration: ^3.1.3  # УДАЛЕНО для веб-версии
  shelf_static: ^1.1.3
  # speech_to_text:  # УДАЛЕНО для веб-версии
  # flutter_tts: ^4.2.2  # Может не работать на веб
  langchain_ollama: ^0.3.0
  langchain: ^0.7.4
  flutter_masked_text2: 
  math_expressions: ^2.6.0
  webview_flutter: ^4.8.0
  fluttertoast: ^8.2.10
  # geocoding: ^3.0.0  # Может не работать на веб
  # geolocator: ^13.0.2  # УДАЛЕНО для веб-версии
  flutter_map: ^7.0.2
  table_calendar: ^3.1.3
  audioplayers: ^6.1.0
  # audioplayers_darwin: ^6.0.0  # УДАЛЕНО для веб-версии
  another_flutter_splash_screen: ^1.2.1
  flutter_screenutil: ^5.9.3
  basic_utils: ^5.7.0
  number_pad_keyboard: ^0.0.6
  # flutter_local_notifications: ^19.0.0  # УДАЛЕНО для веб-версии
  # flutter_timezone: ^4.1.0  # УДАЛЕНО для веб-версии
  intl: ^0.20.2
  loader_overlay: ^5.0.0
  supabase_flutter: ^2.5.6
  # flutter_background_service: ^5.1.0  # УДАЛЕНО для веб-версии
  # workmanager:  # УДАЛЕНО для веб-версии
  teledart: ^0.6.1
    
dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.6.2
  build_runner: ^2.3.3
  freezed: ^2.3.2
  json_serializable: ^6.5.4
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/translations/
  
fonts:
  - family: SourceSansPro
    fonts:
      - asset: assets/fonts/SourceSansProRegular.ttf
      - asset: assets/fonts/SourceSansProSemiBold.ttf
        weight: 600

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/Logo.png"

